import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
    paddingTop: 150,
  },

  textInput: {
    backgroundColor: '#fff',
    width: '90%',
    height: 50,
    marginBottom: 25,
    color: '#222',
    fontSize: 15,
    borderRadius: 10,
    padding: 10,
  },

  image: {
    width: 350,
    height: 350,
    marginBottom:0,
    marginTop: -150
  },
});